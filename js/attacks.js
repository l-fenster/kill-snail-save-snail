class Attacks {
  constructor() {
    const possibleAttacks = [
      "Poison",
      "Aliens",
      "Bird",
      "Salt",
      "Roberto(evil snail)",
    ];
    class Attacks {
      constructor() {
        const randomAttacks =
          possibleAttacks[Math.floor(Math.random() * possibleAttacks.length)];
      }
    }
  }
}
